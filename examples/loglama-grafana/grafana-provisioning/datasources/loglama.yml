apiVersion: 1

datasources:
  - name: LogLama
    type: frser-sqlite-datasource
    access: proxy
    uid: loglama
    isDefault: true
    jsonData:
      path: /logs/loglama.db
    version: 1
    editable: true
